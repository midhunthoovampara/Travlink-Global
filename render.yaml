services:
  - type: web
    name: travlink
    env: dotnet
    plan: free
    buildCommand: dotnet publish Cargon.csproj -c Release -o out
    startCommand: dotnet out/Cargon.dll
    autoDeploy: true
